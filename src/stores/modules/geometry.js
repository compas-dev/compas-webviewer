import compas from "@/api/compas";

// initial state

const state = () => ({});

// getters
// aka: computed state

const getters = {};

// mutations
// aka: handlers of modifications of the state

const mutations = {};

// actions
// aka: methods (e.g. create, read, update, delete)

const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
